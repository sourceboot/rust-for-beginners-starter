# This file tells the `sboot` CLI which SourceBoot course this repo is for.
# Everything else — the tests and the grading engine — lives outside this repo:
# run `sboot where` to see exactly where.
course = "rust-for-beginners"
# Your source lives in game/ for this course.
tree = "game"
