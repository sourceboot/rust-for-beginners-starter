//! lantern — a terminal word-guessing game, written one lab at a time.
//!
//! Each lab adds a module here with a `pub mod` line, and the project text
//! tells you when — lab 01 tells you the first line to add. Until a file is
//! declared here, Rust does not read it at all.
//!
//! `wordlist` is already declared: it is the course's word list, it ships
//! complete, and you never edit it.

pub mod wordlist; // the fixture — ships complete, never edited
